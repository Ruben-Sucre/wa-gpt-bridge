# ── LLM Provider ─────────────────────────────────────────────
# Valores posibles: openai | gemini
LLM_PROVIDER=gemini

# ── OpenAI (solo si LLM_PROVIDER=openai) ─────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# ── Google Gemini (solo si LLM_PROVIDER=gemini) ───────────────
GOOGLE_API_KEY=AIzaSy...
GEMINI_MODEL=gemini-1.5-flash

# ── Redis ─────────────────────────────────────────────────────
# En Docker Compose el hostname es 'redis'
REDIS_URL=redis://redis:6379/0

# ── WhatsApp Cloud API ────────────────────────────────────────
WHATSAPP_TOKEN=EAA_PEGA_TU_TOKEN_AQUI
WHATSAPP_PHONE_ID=tu-phone-id-numerico

# ── Seguridad internal bot ────────────────────────────────────
# Cadena secreta que n8n envía en el header x-bot-secret
BOT_SECRET=cambia-este-secreto
WEBHOOK_VERIFY_TOKEN=cambia-este-verify-token
ALLOW_DIRECT_META_WEBHOOK=false

# ── n8n ───────────────────────────────────────────────────────
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=cambia-este-password
